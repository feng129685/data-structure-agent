# 知识库首批内容模板

这份文档可以直接作为第一批知识库材料上传。建议把每个知识点拆成“一个片段能回答一个具体问题”的形式，方便 aigc.cn 或类似智能体平台做知识库检索/RAG 召回。后续可以继续补充老师的 PPT、实验指导书、课堂例题和小组测试记录。

## 0. 入库规范

### 0.1 推荐片段格式

每个知识库片段尽量包含下面字段。字段名可以保留中文，平台支持元数据时也可以把括号里的英文名作为 metadata key。

| 字段 | 示例 | 用途 |
|---|---|---|
| 标题 `title` | 单链表删除节点为什么需要前驱 | 检索结果标题，最好像学生会问的问题 |
| 主题 `topic` | 链表 | 一级主题，便于按知识点过滤 |
| 适用章节 `chapter` | 第 3 章 线性表 | 对齐老师课程章节 |
| 难度 `difficulty` | 基础 / 提高 / 易错 | 控制回答深度 |
| 关键词 `keywords` | 单链表、删除、前驱、next、头节点 | 提高检索命中 |
| 核心结论 `definition` | 单链表删除节点要修改前驱节点的 next | 让智能体先抓住答案主干 |
| 操作/步骤 `operation` | 找前驱、改 next、处理头节点 | 适合回答“怎么做” |
| 复杂度 `complexity` | 查找 O(n)，已知前驱删除 O(1) | 适合复杂度类问题 |
| 常见误区 `common_error` | 只移动当前指针，没有真正断链 | 适合纠错和追问 |
| 边界情况 `boundary_case` | 空链表、单节点、删除头节点 | 适合测试用例生成 |
| 可视化提示 `visual_hint` | 画 prev -> curr -> curr.next 三个节点 | 引导平台生成图解/动画说明 |
| 示例问法 `sample_questions` | 为什么删除单链表节点要找前驱？ | 覆盖学生可能的口语化提问 |
| 平台标签 `platform_tags` | #线性表 #链表 #指针 #易错点 | 便于平台分类和手动检索 |
| 推荐追问 `follow_up` | 如果删除的是头节点怎么办？ | 引导智能体继续陪练 |
| 来源 `source` | 小组首批整理；待补老师 PPT 页码 | 后续补充课程来源 |

### 0.2 切分规则

| 规则 | 建议 |
|---|---|
| 切分长度 | 每段 300-800 字；含代码时不超过 1200 字 |
| 切分粒度 | 一个概念、一个操作、一个典型题型或一个易错点单独成段 |
| 标题 | 每段必须有清楚标题，例如“链表删除节点的前驱问题” |
| 问法覆盖 | 同一个知识点至少写 2-3 个学生式问法，避免只命中教材原句 |
| 代码 | 代码和解释放在同一段，避免检索到代码却没有上下文 |
| 表格 | 保留复杂度表、操作对比表和易错点表 |
| 来源 | 老师 PPT、讲义、实验指导优先；网上资料只能作补充 |

### 0.3 冲突处理

如果课程资料、教材和通用资料说法不一致，按下面顺序处理：

1. 老师课堂 PPT 或讲义优先。
2. 课程指定教材第二优先。
3. 小组整理资料第三优先。
4. 通用网络资料只作为补充，不当作课程原话。

回答时可以这样说：

```text
按本课程材料，这里采用……的说法。另一些教材可能会把……称为……，但本项目优先使用课程讲义中的表述。
```

## 1. 基础主题速查

### 1.1 栈

- 定义：栈是一种后进先出（LIFO）的线性数据结构。
- 典型操作：`push` 入栈、`pop` 出栈、`top` 查看栈顶、`isEmpty` 判空。
- 常见应用：括号匹配、表达式求值、函数调用栈、深度优先搜索。
- 时间复杂度：入栈、出栈、查看栈顶通常都是 O(1)。
- 易错点：空栈不能出栈；数组实现要注意容量边界；栈顶指针更新顺序容易写错。

### 1.2 队列

- 定义：队列是一种先进先出（FIFO）的线性数据结构。
- 典型操作：`enqueue` 入队、`dequeue` 出队、`front` 查看队首、`isEmpty` 判空。
- 常见应用：广度优先搜索、二叉树层序遍历、任务调度。
- 时间复杂度：入队和出队通常都是 O(1)。
- 易错点：队首和队尾含义混淆；循环队列判空/判满条件写错；出队后忘记更新队首。

### 1.3 链表

- 定义：链表是一种通过指针连接节点的线性数据结构，节点通常包含数据域和指针域。
- 常见类型：单链表、双链表、循环链表。
- 典型操作：插入、删除、遍历、查找。
- 时间复杂度：按位置查找 O(n)；已知前驱时插入/删除 O(1)。
- 易错点：删除节点时丢失后继；头节点、空链表、单节点处理不完整；反转链表时指针顺序错。

### 1.4 二叉树

- 定义：二叉树是每个节点最多有两个孩子的树结构。
- 常见遍历：前序是根-左-右；中序是左-根-右；后序是左-右-根；层序按层从左到右。
- 常用辅助结构：递归遍历依赖函数调用栈；层序遍历通常使用队列。
- 易错点：递归出口缺失；空树没有处理；前序、中序、后序顺序混淆。

### 1.5 图遍历

- 定义：图由顶点和边组成，可以表示复杂关系。
- 常见存储：邻接矩阵、邻接表。
- 常见遍历：BFS 通常用队列；DFS 通常用递归或栈。
- 易错点：忘记标记已访问会重复访问甚至死循环；非连通图只从一个起点遍历会漏顶点。

## 2. 可直接入库的结构化片段

### 片段 1：栈为什么是后进先出

- 主题 `topic`：栈
- 适用章节 `chapter`：第 2 章 线性表 / 栈和队列
- 难度 `difficulty`：基础
- 关键词 `keywords`：栈、LIFO、push、pop、栈顶、括号匹配
- 核心结论 `definition`：栈只允许在栈顶进行插入和删除，所以最后放进去的元素会最先被取出。
- 操作/步骤 `operation`：入栈 `push` 把元素放到栈顶；出栈 `pop` 从栈顶拿走元素；查看 `top` 只看不删。
- 复杂度 `complexity`：顺序栈和链栈在正常实现下，入栈、出栈、查看栈顶都是 O(1)。
- 常见误区 `common_error`：把栈理解成“排队”；空栈还执行 `pop`；更新栈顶后忘记返回原来的栈顶元素。
- 边界情况 `boundary_case`：空栈、只有一个元素、顺序栈已满、连续多次出栈。
- 可视化提示 `visual_hint`：画一个竖直盒子，元素只能从顶部进入和离开；用箭头标出 top 的位置。
- 示例问法 `sample_questions`：
  - 栈为什么叫后进先出？
  - `push` 和 `pop` 到底改了哪里？
  - 括号匹配为什么要用栈？
- 推荐回答 `answer_seed`：
  栈可以想成只能从一端操作的容器，这一端叫栈顶。新元素入栈时放在栈顶，出栈时也只能从栈顶拿，所以最后进去的元素离出口最近，会最先出来。括号匹配适合用栈，是因为最近出现的左括号要先和后面的右括号配对，这正好符合后进先出。
- 平台标签 `platform_tags`：#栈 #线性表 #LIFO #基础概念 #括号匹配
- 推荐追问 `follow_up`：如果表达式里有多种括号，栈怎么判断是否匹配？
- 来源 `source`：小组首批整理；待补老师 PPT 页码

### 片段 2：队列为什么适合做 BFS

- 主题 `topic`：队列
- 适用章节 `chapter`：第 2 章 线性表 / 栈和队列；第 6 章 图
- 难度 `difficulty`：基础
- 关键词 `keywords`：队列、FIFO、BFS、入队、出队、层序遍历
- 核心结论 `definition`：队列先进先出，能保证先发现的节点先被访问，所以适合按层推进的 BFS。
- 操作/步骤 `operation`：起点入队并标记；队首出队；访问它的未访问邻接点；邻接点入队；重复到队空。
- 复杂度 `complexity`：BFS 用邻接表时通常是 O(V+E)，用邻接矩阵时通常是 O(V^2)。
- 常见误区 `common_error`：出队时才标记访问，导致同一节点可能被重复入队；把队列写成栈，遍历顺序变成 DFS。
- 边界情况 `boundary_case`：空图、只有一个顶点、非连通图、存在环的图。
- 可视化提示 `visual_hint`：画一层一层扩散的节点，把队列画成横向管道，左边出队、右边入队。
- 示例问法 `sample_questions`：
  - BFS 为什么要用队列？
  - 队列的先进先出和层序遍历有什么关系？
  - BFS 什么时候标记 visited 比较稳？
- 推荐回答 `answer_seed`：
  BFS 的目标是从起点一圈一圈向外访问。先发现的节点应该先处理，这样才能保证距离起点更近的节点先被访问。队列正好是先进先出：节点发现后从队尾入队，轮到它时从队首出队。为了避免重复入队，一般建议节点一入队就标记已访问。
- 平台标签 `platform_tags`：#队列 #FIFO #BFS #图遍历 #层序遍历
- 推荐追问 `follow_up`：如果图不是连通图，怎样保证 BFS 不漏掉顶点？
- 来源 `source`：小组首批整理；待补老师 PPT 页码

### 片段 3：单链表删除节点为什么需要前驱

- 主题 `topic`：链表
- 适用章节 `chapter`：第 3 章 线性表 / 链式存储
- 难度 `difficulty`：易错
- 关键词 `keywords`：单链表、删除、前驱、next、头节点、指针
- 核心结论 `definition`：单链表节点只知道后继，不知道前驱；删除节点时要让前驱节点的 `next` 跳过当前节点。
- 操作/步骤 `operation`：维护 `prev` 和 `curr`；找到目标节点；执行 `prev->next = curr->next`；如果目标是头节点则单独更新头指针。
- 复杂度 `complexity`：查找目标节点 O(n)；已知前驱后真正断链是 O(1)。
- 常见误区 `common_error`：只写 `curr = curr->next`，这只是移动遍历指针，没有改变链表连接；删除头节点时忘记改 `head`。
- 边界情况 `boundary_case`：空链表、单节点、删除头节点、删除尾节点、目标值不存在。
- 可视化提示 `visual_hint`：画 `prev -> curr -> next` 三个节点，再把 `prev.next` 的箭头直接指向 `next`。
- 示例问法 `sample_questions`：
  - 删除单链表节点为什么要找前驱？
  - `p = p->next` 算不算删除节点？
  - 删除头节点和删除中间节点有什么区别？
- 推荐回答 `answer_seed`：
  在单链表里，节点只能通过 `next` 找到后继，不能反向找到前驱。要把某个节点从链表里拿掉，本质上是修改它前一个节点的 `next`，让链表绕过它。如果只是把当前指针移动到下一个节点，链表原来的连接关系没有变，节点并没有被删除。头节点没有前驱，所以要单独更新 `head`。
- 平台标签 `platform_tags`：#链表 #单链表 #指针 #删除节点 #易错点
- 推荐追问 `follow_up`：如果使用带头结点的单链表，删除逻辑会不会更统一？
- 来源 `source`：小组首批整理；待补老师 PPT 页码

### 片段 4：二叉树层序遍历为什么用队列

- 主题 `topic`：树 / 二叉树
- 适用章节 `chapter`：第 5 章 树和二叉树
- 难度 `difficulty`：基础
- 关键词 `keywords`：二叉树、层序遍历、队列、BFS、根节点、左右孩子
- 核心结论 `definition`：层序遍历要按从上到下、从左到右访问；队列能保存“先发现先访问”的顺序。
- 操作/步骤 `operation`：根节点入队；队首出队并访问；左孩子入队；右孩子入队；重复到队列为空。
- 复杂度 `complexity`：每个节点入队、出队一次，时间复杂度 O(n)，辅助空间最坏 O(n)。
- 常见误区 `common_error`：忘记处理空树；左右孩子入队顺序写反；误用栈导致变成深度优先。
- 边界情况 `boundary_case`：空树、只有根节点、只有左子树、只有右子树、完全二叉树。
- 可视化提示 `visual_hint`：把树按层画出来，在旁边画队列变化：先放根，再放第二层，再放第三层。
- 示例问法 `sample_questions`：
  - 二叉树层序遍历为什么不用栈？
  - 层序遍历的队列里存的是什么？
  - 空树做层序遍历要输出什么？
- 推荐回答 `answer_seed`：
  层序遍历要求同一层从左到右访问，并且上一层处理完后再处理下一层。队列的先进先出可以保留这个顺序：先发现根，再发现根的左右孩子；左孩子先入队，所以也会先出队。遇到空树时直接返回，不需要把空节点放进队列。
- 平台标签 `platform_tags`：#二叉树 #层序遍历 #队列 #BFS #遍历
- 推荐追问 `follow_up`：前序、中序、后序遍历分别适合用什么递归顺序记忆？
- 来源 `source`：小组首批整理；待补老师 PPT 页码

### 片段 5：图遍历为什么必须标记 visited

- 主题 `topic`：图
- 适用章节 `chapter`：第 6 章 图
- 难度 `difficulty`：易错
- 关键词 `keywords`：图、BFS、DFS、visited、重复访问、环、非连通图
- 核心结论 `definition`：图中可能存在环或多条路径到同一顶点，必须用 `visited` 记录访问状态，避免重复访问和死循环。
- 操作/步骤 `operation`：访问或入队一个顶点前先检查 `visited`；确认未访问后再处理；处理后立即标记。
- 复杂度 `complexity`：正确标记后，邻接表遍历通常是 O(V+E)，不会因为重复入队/递归而无限增长。
- 常见误区 `common_error`：认为图像树一样不会回到旧节点；BFS 出队时才标记，导致同一个顶点被多个邻居重复加入队列；非连通图只遍历一个起点。
- 边界情况 `boundary_case`：有环图、自环、重边、非连通图、只有一个顶点。
- 可视化提示 `visual_hint`：给已访问顶点涂色；每访问一个邻接点前先看颜色，已涂色就跳过。
- 示例问法 `sample_questions`：
  - 图遍历为什么要 visited 数组？
  - BFS 是入队时标记还是出队时标记？
  - 非连通图怎么遍历完整？
- 推荐回答 `answer_seed`：
  图和树不一样，图里可能从 A 到 B，又从 B 回到 A，也可能多个顶点都连向同一个顶点。如果不标记访问状态，DFS 可能在环里一直递归，BFS 也可能把同一个顶点重复放进队列。更稳的写法是：节点准备入队或递归访问时就标记 visited。对于非连通图，还要从每个未访问顶点再启动一次遍历。
- 平台标签 `platform_tags`：#图 #BFS #DFS #visited #易错点
- 推荐追问 `follow_up`：邻接表和邻接矩阵写 BFS 时循环部分有什么不同？
- 来源 `source`：小组首批整理；待补老师 PPT 页码

## 3. 常见代码错误库

### 3.1 链表删除节点

- 常见错误：直接移动当前指针，导致无法连接前驱和后继。
- 正确思路：找到待删除节点的前驱；让前驱的 `next` 指向待删除节点的后继；再释放或丢弃待删除节点。
- 适合追问：删除头节点怎么办？带头结点和不带头结点有什么区别？

### 3.2 二叉树递归遍历

- 常见错误：递归出口缺失，或者前序、中序、后序访问位置写错。
- 正确思路：如果当前节点为空，直接返回；再按遍历顺序访问根、左子树、右子树。
- 适合追问：为什么中序遍历二叉搜索树能得到有序序列？

### 3.3 图遍历

- 常见错误：节点出队时才标记访问，导致同一个节点可能被多次入队。
- 更稳妥做法：节点入队时就标记已访问。
- 适合追问：非连通图如何用外层循环补全遍历？

## 4. 导入平台时的标签建议

- 线性表：#线性表 #栈 #队列 #链表 #顺序存储 #链式存储
- 树：#树 #二叉树 #遍历 #递归 #层序遍历
- 图：#图 #BFS #DFS #邻接表 #邻接矩阵 #visited
- 题型：#概念解释 #步骤讲解 #复杂度分析 #易错点 #边界情况
- 场景：#课堂复习 #实验前预习 #代码纠错 #期末复习
