# Historical Project Notes

This directory preserves early proposals, demo scripts, sprint notes, and
prototype reports. They are historical context only and are not the production
source of truth.

Use the root `README.md`, `PRODUCTION_DEPLOYMENT_GUIDE.md`, `contracts/`, and
`deployment/` for the current Structify structure and release procedure.
